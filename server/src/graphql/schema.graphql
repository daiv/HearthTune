type Song {
  id: ID!
  title: String
  duration: Int
  description: String
  played: Int
  local: Boolean
}
type Query {
  search (query: String!, limit:Int):[Song]
  getRelated(id:String, numberOfSongs:Int):[Song]
}