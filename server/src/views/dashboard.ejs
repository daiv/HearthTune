<%- include('partials/header') %>
  <% actions.forEach(action=> { %>
    <div class ="mr-2">
      <a href="<%= action.url %>" class="bg-blue-600 text-white px-4 py-2 rounded-lg inline-block">
        <%= action.name %>
      </a>
    </div>
    <% }) %>

      <%- include('partials/footer') %>